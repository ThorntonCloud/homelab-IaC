apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium-gateway-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ThorntonCloud/homelab-IaC
    targetRevision: dev-cilium
    path: infrastructure/cilium-gateway-config/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
